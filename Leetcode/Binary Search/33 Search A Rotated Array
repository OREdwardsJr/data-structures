class Solution:
    def search(self, nums: List[int], target: int) -> int:
        i = 0
        nums_len = len(nums)
        while i < nums_len:
            if nums[i] == target:
                return i
            i += 1
        return -1